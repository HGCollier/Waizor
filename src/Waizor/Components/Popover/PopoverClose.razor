@namespace Waizor.Components
@inherits SlotBase

@if (AsChild)
{
    @ChildContent(Attributes)
}
else
{
    <button type="button" @attributes="Attributes">
        @ChildContent(Attributes)
    </button>
}