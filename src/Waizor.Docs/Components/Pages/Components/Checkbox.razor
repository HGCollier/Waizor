@page "/checkbox"

<PageTitle>
    Checkbox
</PageTitle>

<label>
    <Waizor.Primitives.Components.Checkbox Required Name="Test" T="string">
        <Waizor.Primitives.Components.CheckboxIndicator T="string">
            @if (context.HasFlag(CheckboxState.Indeterminate))
            {
                @("➖")
            }
            else if (context.HasFlag(CheckboxState.Checked))
            {
                @("✔️")
            }
        </Waizor.Primitives.Components.CheckboxIndicator>
    </Waizor.Primitives.Components.Checkbox>
    Enable notifications
</label>